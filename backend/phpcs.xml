<?xml version="1.0"?>
<ruleset name="PSR12/Laravel">
    <description>PSR12 compliant rules and settings for Laravel</description>

    <arg name="extensions" value="php" />

    <!-- 適用コーディング規約の指定 -->
    <rule ref="PSR12" />

    <!-- 出力に色を適用 -->
    <arg name="colors" />

    <!-- オプション p:進捗表示  s:エラー表示時にルールを表示 -->
    <arg value="ps" />

    <!-- 除外ディレクトリ -->
    <exclude-pattern>/backend/bootstrap/</exclude-pattern>
    <exclude-pattern>/backend/config/</exclude-pattern>
    <exclude-pattern>/backend/database/</exclude-pattern>
    <exclude-pattern>/backend/node_modules/</exclude-pattern>
    <exclude-pattern>/backend/public/</exclude-pattern>
    <exclude-pattern>/backend/resources/</exclude-pattern>
    <exclude-pattern>/backend/routes/</exclude-pattern>
    <exclude-pattern>/backend/storage/</exclude-pattern>
    <exclude-pattern>/backend/vendor/</exclude-pattern>
    <exclude-pattern>/backend/server.php</exclude-pattern>
    <exclude-pattern>/backend/app/Console/Kernel.php</exclude-pattern>
    <exclude-pattern>/backend/tests/CreatesApplication.php</exclude-pattern>
</ruleset>